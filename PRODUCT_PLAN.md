# 基于 ComfyUI 的类 LibTV/Liblib 中文短视频/漫剧制作平台方案

## 1. 总览

产品采用“ComfyUI 生成底座 + 平台产品层”的混合方案。首版不大改 ComfyUI 本体，而是把本机/内网 ComfyUI 作为核心生成后端，平台侧负责工作流注册、任务编排、状态查询、输出归档、中文错误处理、项目资产管理、模板复刻、作品发布和社区互动。后续将稳定的图像、视频、TTS、脚本分析、合成流程逐步沉淀为 ComfyUI 自定义节点、插件或预设工作流包。

前端实现类似 LibTV/Liblib 的产品体验：作品广场、模板/工作流市场、创作工作台、作者主页、短片剧集、动画短片、概念设计、经典衍生、AI 漫剧赛道、成片展示、点赞收藏、封面和最终视频输出。用户在平台化节点画布中编排文本、图片、视频、音频、脚本和生成节点；平台仍只接收业务节点和白名单参数，不允许前端直接提交任意 ComfyUI 原始节点图。

### 当前实现状态

- 已落地可运行 MVP：FastAPI 平台编排层、静态前端原型、Next.js + TypeScript + Tailwind 组件化路由初版、JSON 文件仓储、本地文件存储、Workflow Registry、ComfyUI 任务提交/同步/取消/重试、输出归档、项目工作台、模板市场、作品广场、作品详情、作者主页、互动、发布审核、后台概览、存储清理和中文错误。
- 已覆盖核心创作闭环：创建项目、脚本分析、角色和分镜编辑、手动新增/删除分镜、分镜图/镜头视频/TTS 任务、批量生成、任务队列、素材库、素材类型筛选、素材关键词搜索、时间线和字幕、SRT 导出、成片合成任务、成片归档、提交审核和公开展示。
- 已完成 LibTV 风格节点画布创作端：用户端与后台端分离，`/workspace/[projectId]` 进入全画幅节点画布，可搜索添加、方向键选择节点面板结果、Enter 快速添加当前匹配节点、节点分类筛选、收藏节点、最近使用节点、快速添加上游/下游节点并自动连线、从节点面板拖拽到画布落点创建、空白处右键快捷添加节点、工作流预设和粘贴链路、空白处双击创建、拖入本地或 URL 素材、拖拽、语义输入/输出连接桩连线、自动整理、保存和运行文本、图片、视频、音频、脚本 Beta、分镜图、镜头视频、TTS、合成和演示节点；节点卡片会按节点类型显示提示词、参考图、首帧、视频、音频、字幕、成片等业务端口，同时保留默认输入/输出端口兼容已有工作流；节点图支持后端持久化、本地草稿兜底、视口保存/恢复、视图缩放、视图快速导航、画布视图链接和书签、可导入导出的画布版本快照、画布导航器开关、画布命令面板方向键选择和 Enter 执行、可持久化、可筛选并可导出的画布事件日志、画布批注节点、拖拽框选模式开关、网格吸附、方向键微调节点位置、节点/选区置顶置底、全选/反选/清空选区、上游/下游链路选择、同类型/同状态/同标记/禁用/孤立/起点/终点/错误/提醒/运行中任务/失败任务节点选择、全选/反选连线、连线同起点/同终点/同标签/同颜色/同样式选择与批量编辑、链路/选区系统剪贴板和本地兜底、工作流 JSON 文件追加导入、替换导入和导出、工作流剪贴板导入、选区 JSON 文件导出和可导入导出的本地自定义工作流预设；生成节点已支持分镜下拉绑定、尺寸、seed、首帧、时长、fps、音色、语速、字幕合成等参数预设，并可从上游连线补全分镜、提示词、首帧图片和旁白文本，参数面板也能预览上游输入并一键填充当前节点参数；画布已内置脚本拆解分镜、首帧图生视频、旁白字幕合成三类工作流预设，可一键铺设成组节点和连线，用户也可把当前画布保存为“我的工作流预设”后跨项目复用；画布可打开项目分镜面板，按状态、关键词、排序、多选、未铺设一键选择和铺设状态整理分镜，并可定位已铺设链路，为单个、选中或筛选后的全部分镜一键添加文本、分镜图、视频、配音和合成节点链路；节点卡片、参数面板和素材库可直接预览图片、视频、音频和生成输出素材；节点大纲可按依赖顺序查看节点、搜索节点标题/类型/状态、筛选问题节点、批量选中当前结果或全部问题节点、定位节点和运行任意节点的上游链路；参数面板、连线面板、节点和连线右键快捷菜单支持运行单节点、运行当前上游链路、运行选区、填充上游参数、切换节点类型、编辑节点备注、节点颜色标记、节点折叠/展开、节点参数编辑可撤销、编辑连线标签、复制连线 ID/参数 JSON/定位链接、连线颜色标记、连线样式、调整连线端口映射、反转连线方向、在连线上插入节点、定位连线两端、复制节点、复制节点 ID/参数 JSON/定位链接、框选/多选节点、全选/反选/清空选区、上游/下游链路选择、同类型/同状态/同标记/禁用/孤立/起点/终点/错误/提醒/运行中任务/失败任务节点选择、全选/反选连线、连线同起点/同终点/同标签/同颜色/同样式选择与批量编辑、节点/选区颜色标记、选区批量命名、选区批量应用生成参数预设、选中分镜绑定到选区节点、节点/选区折叠/展开、节点/选区断开连线、复制/剪切选区到系统剪贴板并粘贴到当前视图中心或画布右键位置、保存选区为预设、运行选区、串联选区、选区生成视频链、选区生成配音链、按媒体类型汇聚选区到合成节点、选区接入已有合成节点、整理选区、选区左/右/水平居中/顶部/底部/垂直居中对齐与等距分布、选区打组/编辑分组名称/取消分组、选区内部连线批量启用/禁用、颜色标记、样式和删除、批量锁定/解锁节点、批量启用/禁用节点、复制上游链路到系统剪贴板并粘贴到当前视图中心或画布右键位置、锁定/解锁节点、启用/禁用节点、启用/禁用连线、删除连线和删除节点，节点类型切换会保留已有参数和连线，锁定节点会禁止误拖动与误删，禁用节点会保留画布结构但在链路和全图运行时跳过，禁用连线会从上游输入、整理和运行中排除，画布会阻止自连和重复端口连线；并可查看选中节点关联任务的进度、错误、重试建议、事件日志、ComfyUI prompt_id 和当前节点运行前缺口；任务队列支持状态统计、状态筛选、任务关键词搜索、定位关联节点、提交、同步、重试和取消任务，并支持对当前筛选任务批量同步、批量重试失败任务和一键选中筛选任务关联节点；画布自检可在运行前检查断线、孤立节点、禁用节点、禁用连线、自连/重复连线、缺少分镜、提示词、首帧、配音文本和合成输入等问题并定位到节点，运行单节点、链路和全图前会拦截阻断性自检问题并提示处理，画布批注节点仅用于协作说明且会从自检孤立节点提示和运行队列中跳过；画布支持保存、撤销、重做、运行全图、整理、视图缩放、导入导出工作流、复制、剪切、粘贴、删除等键盘快捷操作。
- 本次已完成：任务队列批量操作补齐筛选任务提交、筛选任务同步、筛选失败任务重试、筛选待生成/生成中任务取消和筛选任务关联节点选中，命令面板与任务队列浮层均可直接执行。
- 本次已完成：全画幅画布支持对选中生成节点一键生成变体，自动重置任务/输出状态、为分镜图变体随机 seed，并保留外部上游参考输入和选区内部连线，便于同一提示词或素材快速试多个版本。
- 本次已完成：全画幅画布支持选区网格排列，可将多选节点按行列展开，便于对比生成变体、批量素材和分镜链路；命令面板、右键菜单和批量操作区均可执行。
- 本次已完成：全画幅画布支持选区分镜图随机 seed 重跑，可批量刷新分镜图生成节点 seed，并清空旧任务、输出和错误状态，便于快速重新生成多版画面。
- 本次已完成：全画幅画布支持清空选区生成结果，可批量清除分镜图、视频、配音和合成节点的任务、输出与错误状态，方便调参后重新运行。
- 本次已完成：全画幅画布合成节点支持编辑单镜头时长、字幕样式、转场方式、合成音色和 BGM URL；运行合成节点时这些参数会写入成片任务，并在时间线/字幕重建和工作流注册表校验中生效。
- 本次已完成：对齐 Liblib 首页快速体验模型入口，首页、创作工作台和全画幅画布新增 Seedance 2.0 快速体验；创建后会直接进入画布并自动铺设参考图、动作提示词、图生视频和成片合成节点链路。
- 本次已完成：对齐 Liblib 首页 TV Show 入口，首页分类、创作入口和全画幅画布新增 TV Show 剧集开场预设；创建后自动铺设剧集脚本、分镜图、镜头视频、主持人口播和成片合成链路。
- 本次已完成：对齐 Liblib 首页创作者挑战赛入口，首页分类、示例作品、创作入口和全画幅画布新增创作者挑战赛参赛片预设；创建后自动铺设赛题 brief、参赛海报首帧、短片镜头、宣发口播和成片提交链路。
- 本次已完成：对齐 Liblib 首页“全部 / 创作者挑战赛 / Seedance 2.0 / TV Show”快捷频道，首页可直接筛选全部、挑战赛和 TV Show 作品，并通过快捷入口直接创建项目进入对应全画幅创作画布。
- 本次已完成：首页频道栏补齐真正的 Seedance 2.0 频道筛选，并按当前频道展示对应创作入口，本地示例也新增 Seedance 2.0 作品用于筛选预览。
- 本次已完成：作品广场筛选区补齐当前频道同款创作条，可按全部、创作者挑战赛、Seedance 2.0 和 TV Show 直接进入对应创作画布。
- 本次已完成：首页 TV Show 频道补齐本地示例作品，四个 Liblib 快捷频道在离线兜底状态下都有可预览内容。
- 本次已完成：模板市场兜底数据补齐 Seedance 2.0 快速体验模板，可从模板市场直接进入 Seedance quick 同款创作。
- 本次已完成：模板市场兜底数据补齐 TV Show 剧集开场模板，可从模板市场直接进入 TV Show quick 同款创作。
- 本次已完成：模板市场兜底数据补齐创作者挑战赛参赛片模板，可从模板市场直接进入挑战赛 quick 同款创作。
- 本次已完成：模板市场补齐 Liblib quick 频道筛选，可按全部、创作者挑战赛、Seedance 2.0 和 TV Show 浏览模板，并从当前频道直接开始创作。
- 本次已完成：首页创作工作台补齐挑战赛、Seedance 2.0 和 TV Show 的 quick 入口，点击后直接创建项目并进入对应 Liblib 风格全画幅创作画布。
- 本次已完成：创建页补齐 Liblib 快捷创作模式切换区，可直接在 Seedance 2.0、TV Show、创作者挑战赛和普通创建之间切换，并同步标题、脚本和主创建按钮。
- 本次已完成：创建页 quick 模式补齐推荐画幅自动应用，Seedance 2.0 和创作者挑战赛默认竖屏，TV Show 默认横屏，减少进入全画幅画布前的手动设置。
- 本次已完成：创建页 quick 模式补齐项目类型自动同步，切换 Seedance 2.0、TV Show 或创作者挑战赛时下拉类型会跟随当前同款创作模式。
- 本次已完成：Seedance 2.0 quick 创建会把创建页参考图 URL 带入全画幅画布，并覆盖 Seedance 预设参考图节点，保持图生视频入口参数连续。
- 本次已完成：创建页 quick 模式会把用户编辑后的 Seedance 动作提示词、TV Show 剧集脚本和创作者挑战赛 brief 带入全画幅画布，覆盖对应预设节点文案。
- 本次已完成：创建页 Seedance 2.0 quick 模式直接显示参考图 URL 输入，用户可在进入全画幅画布前调整图生视频参考图。
- 本次已完成：Liblib 同款创作链接会携带来源作品/模板上下文，创建页显示同款来源，并在进入全画幅画布后自动生成来源批注节点，方便按原作品继续改节点。
- 本次已完成：Liblib 同款创作链接会携带模板示例提示词、示例首帧/参考图或作品封面，创建页自动预填脚本/动作提示词和 Seedance 参考图，再带入全画幅画布预设与来源批注。
- 本次已完成：作品广场和模板市场的同款创作按钮可直接创建项目并进入全画幅预设画布，自动携带 quick 预设、来源提示词、参考图和来源批注，减少从卡片到节点画布的中间步骤。
- 本次已完成：作品详情页和作者主页的作品/模板同款创作入口也可直接创建项目并进入全画幅预设画布，和作品广场、模板市场保持同一套同款创作链路。
- 本次已完成：普通模板同款创作会携带 workflow_key，并把 Flux 分镜图、Wan2.1 镜头视频和 TTS 模板映射到脚本拆解、图生视频或旁白合成全画幅预设，模板示例输入会直接覆盖对应节点。
- 本次已完成：Flux 分镜图类普通模板进入全画幅脚本拆解预设时，会把模板示例提示词同步到脚本节点和分镜图 prompt，并把示例首帧/参考图写入分镜图参考图参数。
- 本次已完成：Wan2.1 镜头视频和 TTS 普通模板同款创作会把默认时长、fps、音色和语速随来源参数带入全画幅视频生成或旁白配音节点，并写入来源批注。
- 本次已完成：首页顶部和频道条的创作者挑战赛、Seedance 2.0、TV Show 快捷入口可直接创建项目并进入对应全画幅预设画布，不再先落到创建页。
- 本次已完成：全局侧边栏快捷创作的创作者挑战赛、Seedance 2.0、TV Show 入口可直接创建项目并进入对应全画幅预设画布，和首页快捷入口保持一致。
- 本次已完成：首页创作工作台和作品广场频道 CTA 的挑战赛、Seedance 2.0、TV Show 快捷入口可直接创建项目并进入全画幅预设画布。
- 本次已完成：模板市场频道 CTA 的全部、创作者挑战赛、Seedance 2.0、TV Show 快捷入口可直接创建项目并进入全画幅预设画布。
- 本次已完成：模板复刻侧栏创建项目成功后会直接进入全画幅画布，不再停留在模板市场等待手动点击进入项目。
- 本次已完成：首页顶部和全局侧边栏的“开始创作”会直接创建空白项目并进入全画幅画布，不再先进入创建页。
- 本次已完成：首页创作工作台“脚本成片”会直接创建脚本成片项目并进入脚本拆解分镜全画幅预设画布。
- 本次已完成：全画幅画布和旧项目工作台的返回入口回到作品广场，创建页项目跳转文案改为“进入全画幅画布”，避免继续暴露旧工作台/创建页导向。
- 本次已完成：发布导出组件在无最近项目时回到作品广场，不再把合成、字幕和提交审核入口兜底到创建页。
- 本次已完成：同步修正产品计划中仍写着“进入创建页 / 项目工作台跳转 / 无项目回到创建入口”的过时说明，明确首页主入口默认直达全画幅画布。
- 本次已完成：作品广场卡片和作品详情页补齐分享链接复制能力，剪贴板不可用时会把作品分享链接暂存到本地。
- 本次已完成：模板市场卡片补齐模板分享链接复制能力，可复制带模板定位参数的市场链接，剪贴板不可用时会把分享链接暂存到本地。
- 本次已完成：模板市场分享链接支持打开后自动定位模板卡片，必要时切回全部频道并高亮目标模板，便于从分享入口直接复刻或同款创作。
- 本次已完成：作者主页公开作品和发布模板列表补齐分享能力，可直接复制作品详情链接或模板定位链接，剪贴板不可用时会暂存到本地。
- 本次已完成：作者主页头部补齐主页分享能力，可直接复制作者主页链接，剪贴板不可用时会暂存到本地。
- 本次已完成：作者主页发布模板的标题和“查看模板市场”入口会直接跳转到模板市场定位链接，和模板分享链接保持一致，便于从作者主页追到具体模板继续复刻。
- 本次已完成：作品广场卡片作者信息改为可点击作者主页入口，用户可从作品列表直接进入作者主页继续关注、复刻或分享。
- 本次已完成：作品详情页模板来源改为可点击模板市场定位链接，用户可从作品详情直接追到对应模板继续复刻或同款创作。
- 本次已完成：模板市场补齐关键词搜索，可按模板名、描述、workflow key 和适用场景筛选模板，并保留频道筛选组合能力。
- 本次已完成：模板市场筛选会在独立模板页同步写回 URL，并支持从 URL 恢复频道和关键词、一键清空筛选与当前筛选提示，便于分享模板频道或搜索结果。
- 本次已完成：作品详情页分类和标签可返回作品广场筛选同类内容，首页作品广场支持从 URL 读取 category、keyword 和 sort_by 查询参数。
- 本次已完成：作品广场搜索输入会随 URL keyword 和外部筛选状态同步，避免从详情标签返回后输入框与实际筛选条件不一致。
- 本次已完成：作品广场筛选操作会同步写回 URL，并补齐当前筛选提示和一键清空筛选，用户从详情标签、频道或搜索进入后可快速回到全部作品。
- 本次已完成：首页创作工作台创建空白项目后会直接进入全画幅画布，保持开始创作即进入节点创作环境的体验。
- 本次已完成：全局侧边栏补齐 Liblib 快捷创作分组，在任意页面都可直达创作者挑战赛、Seedance 2.0 或 TV Show quick 创建入口。
- 本次已完成：对齐 Liblib 作品卡片复刻体验，作品广场卡片新增“同款创作”和“查看详情”，可根据作品分类、模板名或标签跳转到挑战赛、TV Show、Seedance 2.0 或普通创建入口。
- 本次已完成：全画幅画布补齐 Liblib 同款创作参数预设，分镜图、镜头视频、配音和成片合成节点可一键套用漫剧同款、TV Show、Seedance 运镜和挑战赛包装，并支持选区批量应用。
- 本次已完成：作品详情页补齐 Liblib 同款创作入口，详情页主操作区和模板信息区可复用作品分类、模板和标签映射，直接跳转挑战赛、TV Show、Seedance 2.0 或普通创建模式。
- 本次已完成：模板市场补齐 Liblib 模板同款创作入口，模板卡片可按分类、名称、workflow key 和适用场景直接创建挑战赛、TV Show、Seedance 2.0 或普通模板复刻全画幅画布。
- 本次已完成：作者主页补齐 Liblib 同款创作入口，作者公开作品和发布模板列表可直接复用作品或模板映射进入同款创作，也保留详情页和模板市场跳转。
- 本次已完成：创建页 quick 模式主按钮直达对应全画幅预设，从作品、模板或作者页进入 Seedance 2.0、TV Show、创作者挑战赛后可一键创建并进入节点画布，无需再识别下方快捷按钮。
- 本次已完成：创建页 quick 模式进入全画幅画布时会携带替换模式，画布自动替换为 Seedance 2.0、TV Show 或创作者挑战赛完整链路，避免开始创作时叠加到旧草稿。
- 本次已完成：全画幅画布顶部补齐 Liblib 快捷创作入口，可随时追加 Seedance 2.0、TV Show 和创作者挑战赛节点链路，便于在已有画布中继续扩展同款创作。
- 本次已完成：画布命令面板补齐 Liblib 快捷创作命令，可搜索并追加 Seedance 2.0、TV Show 和创作者挑战赛预设链路，和顶部快捷按钮保持一致。
- 本次已完成：画布右键快捷菜单补齐 Liblib 快捷创作分组，可在鼠标当前位置直接追加 Seedance 2.0、TV Show 或创作者挑战赛链路。
- 本次已完成：左侧节点/预设面板补齐 Liblib 快捷创作区，打开面板即可一键追加 Seedance 2.0、TV Show 或创作者挑战赛链路。
- 本次已完成：全画幅画布补齐 Liblib 开始创作式新建入口，节点面板和命令面板可一键替换当前画布为 Seedance 2.0、TV Show 或创作者挑战赛完整链路，替换前会写入撤销历史。
- 本次已完成：全画幅画布支持按当前节点绑定的分镜一键选中同分镜节点，便于批量调整同一分镜的文本、分镜图、视频和配音链路。
- 本次已完成：全画幅画布支持把当前节点业务参数同步到同一分镜下的其它同类型节点，避免误复制任务状态和输出，便于统一同镜头提示词、尺寸、首帧、音色或合成参数。
- 本次已完成：全画幅画布支持把当前节点业务参数批量应用到选区内其它同类型节点，复用同一套白名单字段，便于多节点快速统一提示词、媒体 URL、生成尺寸、视频参数、配音参数和字幕开关。
- 本次已完成：全画幅画布和默认图像/视频生成工作流支持负面提示词，可从分镜写入、在节点参数面板编辑、随同分镜或选区参数同步，并在运行生成节点时作为平台白名单业务参数提交。
- 本次已完成：全画幅分镜图生成节点支持参考图 URL，可在节点参数面板编辑、从上游图片节点补全，并通过默认 Flux 工作流白名单参数提交，便于保持角色、风格或构图一致。
- 本次已完成：全画幅生成节点支持在参数面板指定工作流 Key，留空时继续使用项目默认工作流，并可随同分镜或选区参数同步，便于同一画布内切换不同图像、视频、配音或合成工作流。
- 本次已完成：全画幅分镜图生成节点支持模型 Key 和统一风格提示词，可在参数面板编辑、随同分镜或选区参数同步，并通过默认 Flux 工作流白名单参数提交，便于按节点切换模型和统一画面风格。
- 本次已完成：全画幅分镜图生成节点支持生成数量 batch_size，可在参数面板设置单次出图数量、随同分镜或选区参数同步，并通过默认 Flux 工作流白名单参数提交，便于同一提示词一次生成多张候选图。
- 本次已完成：全画幅镜头视频生成节点支持运镜方式 camera_motion 和运动强度 motion_strength，可在参数面板编辑、随同分镜或选区参数同步，并通过默认 Wan2.1 工作流白名单参数提交，便于按节点控制镜头推进、横移、环绕或固定镜头。
- 本次已完成：全画幅旁白配音节点支持情绪 emotion 和音调 pitch，可在参数面板编辑、批量生成或选区参数同步，并通过默认 TTS 工作流白名单参数提交，便于控制旁白的平稳、轻快、低落或激烈表现。
- 已做稳定性边界：项目作者权限、审核角色权限、可选平台 API 访问令牌、账号注册、密码登录、通用 OAuth/OIDC 第三方登录入口、轻量 HMAC 会话令牌与刷新、会话身份中间件、请求体/query 用户冒用拦截、基础限流、Prometheus 文本指标、Webhook 主动告警、告警 Webhook 探针、告警指纹冷却降噪、静态前端托管、CORS 白名单配置、JSON 文件仓储与可选 PostgreSQL JSONB 仓储、PostgreSQL 关系投影表与高频查询索引、本地归档与 S3/OSS/COS/MinIO 兼容对象存储上传适配、对象存储厂商配置校验、管理员存储读写探针、管理员工作流注册表探针、管理员支付回调探针、管理员打款 Webhook 探针、可选 Redis/arq 任务投递、arq 常驻 worker 入口、Docker Compose 和 systemd 部署模板、运行中任务批量同步、轻量 worker CLI、ComfyUI 图像/视频/TTS/合成基础自定义节点包、ComfyUI 插件安装校验工具、ComfyUI HTTP/API Key/队列状态处理、ComfyUI `/view` 远端输出下载兜底、任务失败诊断、存储路径安全、孤儿文件清理、素材引用完整性检查、内部积分账户、生成任务扣费、运营调账、支付订单、支付收银台 URL 模板、签名支付回调、支付成功幂等入账、作品收益分账、业务参数白名单和数值/布尔参数严格校验。
- 仍属生产化目标：更多真实表单提交流程、具体第三方平台联调、对象存储真实厂商账号联调、更多告警渠道真实机器人联调、更多 ComfyUI 生产 workflow 节点适配和安装联调、微信/支付宝/Stripe 等真实支付账号联调，以及会员订阅收单、提现打款等外部渠道账号联调。

## 2. 核心产品模块

- 首页/作品广场：瀑布流展示作品，支持分类筛选：短片剧集、动画短片、概念设计、经典衍生、AI 漫剧、广告短片、精选画布。
- 创作工作台：项目创建、脚本输入、角色设定、分镜拆解、镜头生成、配音字幕、视频合成、导出发布。
- 模板/工作流市场：展示可复用的创作模板，模板包含 ComfyUI 工作流、默认参数、示例输入、封面、成片示例。
- 作者体系：作者主页、作品列表、模板列表、点赞数、收藏数、粉丝数、作者等级：普通、先锋、专业。
- 项目资产库：统一管理脚本、角色图、分镜图、视频片段、音频、字幕、封面、最终成片。
- 审核与发布：作品先进入草稿、待审核、已发布、已下架状态；平台广场只展示已发布内容。

## 3. 技术架构

- 前端产品层：生产目标使用 Next.js + TypeScript + Tailwind CSS，所有用户可见文案使用中文；首屏直接进入作品广场和创作入口，不做营销落地页。当前 `frontend-next/` 已拆分 `AppShell`、作品广场、作品详情、作者主页、创作工作台、独立项目工作台、模板市场、积分充值、账号令牌、发布审核等组件，并提供 `/create`、`/workspace/[projectId]`、`/templates`、`/billing`、`/account`、`/account/oauth/callback`、`/works/[id]`、`/users/[id]`、`/admin/review` 核心路由；作品广场卡片展示封面、成片标记、模板来源、标签和互动计数，并已将分类筛选、关键词搜索和排序接入 `/api/works` 查询参数，首页工作台已接入项目草稿列表、脚本成片直达画布、快速创建空白项目和全画幅画布跳转，首页发布导出区会读取当前用户最近项目，并提供合成、字幕导出、提交审核和审核队列导航，无项目时回到作品广场；`/create` 作为高级创建兼容页已接入项目创建、脚本分析、图片成片参考图、空白项目创建、模板列表读取、模板选择、目标画幅、分镜图生成、批量生成、时间线和合成任务 API，`/workspace/[projectId]` 已切换为全画幅节点创作画布，支持节点搜索面板、方向键选择节点面板结果、Enter 快速添加当前匹配节点、节点分类筛选、收藏节点、最近使用节点、快速添加上游/下游节点并自动连线、节点面板拖拽添加、节点大纲搜索、问题筛选和批量选中、语义输入/输出连接桩、节点右键快捷菜单、连线右键快捷菜单、选区批量操作、选区批量命名、选区批量应用生成参数预设、选中分镜绑定到选区节点、选区运行、选区一键串联、选区生成视频链、选区生成配音链、选区按媒体类型汇聚到合成节点、选区接入已有合成节点、选区左/右/水平居中/顶部/底部/垂直居中对齐与等距分布、选区打组/选中同组节点/分组名称编辑/取消分组、选区内部连线批量启用/禁用、颜色标记、样式和删除、连线标签编辑、复制连线 ID/参数 JSON/定位链接、连线颜色标记、连线样式、连线端口映射、连线插入节点、连线反转方向、连线启用/禁用、全选/反选连线、连线同起点/同终点/同标签/同颜色/同样式选择与批量编辑、工作流预设、我的工作流预设、画布自检、项目分镜面板、分镜状态筛选、分镜关键词搜索、分镜排序、分镜多选、未铺设分镜一键选择、分镜铺设状态识别与筛选、已铺设分镜链路定位、按单个、选中或筛选后的全部分镜添加生成链路、分类添加、空白处右键快捷添加节点、工作流预设和粘贴链路、空白处双击添加画布批注、素材拖入、素材预览、素材类型筛选、素材关键词搜索、筛选素材批量添加到画布、任务状态筛选、任务关键词搜索、上游输入预览和参数填充、选区复制/剪切、粘贴到当前视图中心或画布右键位置、选区保存为预设、链路复制粘贴到当前视图中心或画布右键位置、工作流 JSON 文件追加导入、替换导入和导出、工作流剪贴板导入、选区 JSON 文件导出、视口保存/恢复、视图缩放、视图快速导航、画布视图链接和书签、可导入导出的画布版本快照、画布导航器开关、画布命令面板方向键选择和 Enter 执行、可持久化、可筛选并可导出的画布事件日志、画布批注节点、拖拽框选模式开关、网格吸附、方向键微调节点位置、节点/选区置顶置底、全选/反选/清空选区、上游/下游链路选择、同类型/同状态/同标记/禁用/孤立/起点/终点/错误/提醒/运行中任务/失败任务节点选择、全选/反选连线、连线同起点/同终点/同标签/同颜色/同样式选择与批量编辑、自动整理、选区整理、撤销/重做、运行全图、缩放键盘快捷操作、分镜绑定、节点参数编辑、节点颜色标记、节点折叠/展开、节点参数编辑可撤销、生成参数预设、节点备注、节点类型切换、节点锁定/解锁、节点启用/禁用、批量锁定/启用/禁用节点、运行诊断、任务队列状态统计、状态筛选、任务关键词搜索、任务定位、提交/同步/重试/取消、画布保存、节点运行、链路运行、节点复制、节点/选区断开连线、删除和连线补全；`/templates` 已接入 `/api/templates` 并支持模板复刻创建项目，复刻时可填写项目标题和目标画幅，模板卡片展示适用场景、默认参数、示例输入、封面、成片示例和使用次数，`/works/[id]` 已接入 `/api/works/{work_id}` 和作品点赞/收藏互动，`/users/[id]` 已接入 `/api/users/{user_id}` 和关注作者互动，`/admin/review` 已接入未发布作品队列、作品审核通过/驳回/下架、待审核提现队列、打款通知待处理队列、提现通过/驳回、打款通知重试、运营概览、部署自检、平台健康、工作流注册表探针、存储读写探针、支付回调探针、告警 Webhook 探针、打款 Webhook 探针、存储清理和运行中任务同步 API，`/billing` 已接入 `/api/billing/account`、`/api/billing/payment-orders`、`/api/billing/subscriptions`、`/api/billing/withdrawals`、`/api/admin/billing/credits`、`/api/admin/billing/works/{work_id}/revenue`、`/api/admin/billing/withdrawals/{withdrawal_id}/review` 和 `/api/admin/billing/withdrawals/{withdrawal_id}/retry-payout`，覆盖积分账户、支付订单、支付收银台入口、会员订阅、创作者提现、运营提现审核、运营调账、打款通知重试和作品收益分账，`/account` 已接入 `/api/auth/register`、`/api/auth/login`、`/api/auth/oauth/{provider}/start`、OAuth 回调会话保存、`/api/auth/session/refresh` 和 `/api/auth/session/me`；Next 运行时通过 `PLATFORM_API_BASE_URL` 将 `/api/*` 和 `/storage/*` 代理到 FastAPI，并已加入 API 兼容测试服务 + Next smoke 联调，覆盖代理、项目创建/读取、作品查询参数、模板复刻参数、支付订单创建、会员订阅、提现申请、后台提现队列、打款通知重试、支付回调探针、告警 Webhook 探针、打款 Webhook 探针、工作流注册表探针、部署自检、存储读写探针、OAuth 发起登录、OAuth 回调页和核心页面访问；前端依赖已生成 `package-lock.json`，并通过 TypeScript 检查、Next 生产构建和运行时 smoke。保留 `frontend/` 静态原型作为 MVP 可运行入口。
- 平台编排层：FastAPI 作为轻量业务 API，不承载模型推理；负责用户、项目、分镜、任务、素材、作品、模板、互动、审核和权限。
- 生成后端：ComfyUI 是首版唯一默认生成后端，平台通过 HTTP/WebSocket 调用 ComfyUI 的 prompt、history、queue、view 等接口提交任务、轮询状态、读取输出。
- 数据库：默认 JSON 文件仓储用于开发；生产可通过 `PLATFORM_REPOSITORY_DRIVER=postgres` 切换到 PostgreSQL JSONB 仓储，存储平台业务数据、任务状态、ComfyUI prompt_id、workflow_key、输入参数、输出资产、审核状态和互动计数。PostgreSQL 模式会同步维护关系投影表和高频查询索引，覆盖项目作者、任务项目/状态、作品审核/分类、模板 workflow、互动目标、素材来源任务等关联查询。
- 队列：Redis + arq 用于平台侧异步编排，包括 ComfyUI 任务提交、状态同步、输出归档、视频合成、发布审核和失败重试。
- 文件存储：开发期本地 `storage/`，生产期抽象为 S3/OSS 兼容存储；数据库只存文件 URL、hash、mime、尺寸、时长。
- Workflow Registry：平台维护工作流注册表，声明 `workflow_key`、ComfyUI workflow JSON、参数 schema、默认参数、输出节点映射、适用场景和中文说明。
- ComfyUI 改造路径：首版通过外接工作流集成；当工作流稳定后，再封装为 ComfyUI 自定义节点、插件、工作流包或模型预设，减少平台侧对节点 ID 的硬编码。
- 可复用能力：优先复用 Pixelle-Video 的 `workflows/selfhost/*`、`workflows/runninghub/*`、任务状态模型、TTS/图像/视频生成服务。

## 4. 数据模型

- `User`：昵称、头像、简介、作者等级、角色、状态。
- `Project`：标题、类型、状态、作者、当前步骤、封面、成片地址、创建/更新时间。
- `Script`：原始文本、改写文本、风格、目标时长、语言。
- `Character`：角色名、设定、参考图、统一风格提示词、LoRA/模型配置。
- `StoryboardShot`：镜头序号、旁白、画面描述、景别、角色、提示词、负面提示词、生成状态、关联素材。
- `SubtitleCue`：字幕序号、起止时间、字幕文本、字幕样式、关联项目和分镜。
- `TimelineItem`：时间线序号、起止时间、镜头视频、配音素材、字幕 cue、转场方式、关联项目和分镜。
- `GenerationTask`：任务类型、状态、进度、错误、ComfyUI prompt_id、输入参数、输出文件。
- `Asset`：类型、URL、本地路径、mime、宽高、时长、hash、来源任务。
- `WorkTemplate`：名称、描述、分类、封面、示例视频、workflow key、参数 schema、默认参数、示例输入、适用场景、发布状态。
- `PublishedWork`：标题、描述、封面、视频、分类标签、作者、模板来源、审核状态、点赞数、收藏数、浏览数。
- `Interaction`：用户、目标类型、目标 ID、类型：like/favorite/follow。

## 5. API 接口

- `POST /api/projects`：创建项目，输入标题、类型、目标比例、默认模板。
- `POST /api/auth/register` / `POST /api/auth/login` / `POST /api/auth/session/refresh` / `GET /api/auth/session/me`：注册、密码登录、刷新和校验会话。
- `GET /api/auth/oauth/{provider}/start` / `GET /api/auth/oauth/{provider}/callback`：通用 OAuth/OIDC 第三方登录入口，支持签名 state、授权码换 token、userinfo 拉取和平台会话签发。
- `GET /api/projects` / `GET /api/projects/{id}`：项目列表和详情。
- `GET /api/projects/{id}/assets` / `DELETE /api/projects/{id}/assets/{asset_id}`：查询和清理项目归档素材。
- `POST /api/projects/{id}/script/analyze`：脚本分析，生成角色、剧情段落和分镜草稿。
- `PATCH /api/projects/{id}/characters/{character_id}`：编辑项目角色设定、参考图和统一风格提示词。
- `POST /api/projects/{id}/shots`：手动新增分镜，用于空白项目或补充分镜。
- `PATCH /api/projects/{id}/shots/{shot_id}`：编辑分镜旁白、画面描述、提示词、负面提示词和出场角色。
- `DELETE /api/projects/{id}/shots/{shot_id}`：删除分镜并清理关联任务、素材、时间线和字幕草稿。
- `POST /api/projects/{id}/shots/{shot_id}/generate-image`：生成单个分镜图。
- `POST /api/projects/{id}/shots/{shot_id}/generate-video`：生成单个镜头视频。
- `POST /api/projects/{id}/shots/{shot_id}/generate-tts`：生成单个分镜旁白配音。
- `POST /api/projects/{id}/batch-generate`：批量创建分镜图或旁白配音任务。
- `POST /api/projects/{id}/timeline/build`：根据分镜顺序生成时间线草稿和字幕 cue，并绑定已有视频/音频素材。
- `PATCH /api/projects/{id}/subtitles/{subtitle_id}`：编辑字幕文本、样式和起止时间。
- `POST /api/projects/{id}/subtitles/export`：导出 SRT 字幕文件并登记为项目素材。
- `POST /api/projects/{id}/compose`：合成字幕、配音、BGM、镜头片段，输出成片。
- `GET /api/tasks/{task_id}`：查询生成/合成进度。
- `GET /api/comfy/status`：检查 ComfyUI 连接状态、队列状态和系统信息，未连接时返回中文错误。
- `GET /api/health`：平台健康检查，汇总 ComfyUI 连接、失败任务、缺失素材、待审核积压和运营告警。
- `GET /api/metrics`：Prometheus 文本指标，暴露 ComfyUI 连接、队列、项目、任务、素材、审核、存储和引用完整性指标。
- `GET /api/workflows`：读取平台已注册的 ComfyUI 工作流、参数 schema、默认参数和适用场景。
- `POST /api/comfy/tasks/{task_id}/sync`：同步指定 ComfyUI 任务状态和输出文件，用于重试或人工修复。
- `POST /api/works/{project_id}/publish`：提交发布审核。
- `POST /api/admin/review/{work_id}`：运营/审核角色执行作品通过、驳回或下架。
- `GET /api/admin/overview`：后台运营概览，统计项目、任务、素材、存储和待审核情况。
- `GET /api/admin/runtime-config`：后台部署自检，脱敏返回 ComfyUI、ComfyUI 平台插件、工作流注册表、仓储、存储、队列、鉴权、告警、支付、打款和前端托管配置状态，并给出生产就绪检查项、阻塞项和警告项。
- `POST /api/admin/workflows/probe`：运营/审核角色执行工作流注册表探针，验证参数 schema、adapter 输出节点映射、生成类型覆盖和 ComfyUI 提交 payload 构建。
- `POST /api/admin/alerts/probe`：运营/审核角色发送一条告警 Webhook 探针，用于验证告警机器人地址、渠道格式和签名配置。
- `POST /api/admin/comfyui/plugin/install`：后台安装平台 ComfyUI 自定义节点插件到 `COMFYUI_ROOT/custom_nodes/video_gen_platform_nodes`，支持覆盖安装并返回节点清单。
- `POST /api/admin/storage/probe`：运营/审核角色执行一次存储读写探针，验证本地归档写入、S3 兼容上传和探针清理链路。
- `POST /api/admin/storage/cleanup`：运营/审核角色清理未被素材记录引用的孤儿归档文件，并返回缺失素材报告。
- `POST /api/admin/tasks/sync-running`：运营/审核角色批量同步运行中的 ComfyUI 任务，支持预检和数量限制。
- `GET /api/billing/account`：查询当前用户积分账户、余额和最近流水。
- `POST /api/billing/payment-orders`：创建积分充值支付订单，可通过通用或渠道专用 checkout URL 模板生成收银台入口。
- `POST /api/billing/payment-webhook/{channel}`：第三方支付渠道签名回调，确认成功后幂等入账积分。
- `GET /api/billing/subscriptions` / `POST /api/billing/subscriptions`：查询和开通会员订阅，开通时扣除积分并写入流水。
- `GET /api/billing/withdrawals` / `POST /api/billing/withdrawals`：查询和提交创作者提现申请，申请时冻结积分并进入待审核状态。
- `POST /api/admin/billing/credits`：运营/审核角色为用户充值、扣减或修正积分，并写入审计流水。
- `POST /api/admin/billing/works/{work_id}/revenue`：运营/审核角色为已发布作品记录收益，按平台比例拆分作者收益和平台收益。
- `POST /api/admin/billing/payment-webhook/probe`：运营/审核角色创建小额测试支付订单，生成平台签名并走真实支付回调确认逻辑，用于验证支付密钥、签名和入账链路。
- `POST /api/admin/billing/withdrawals/{withdrawal_id}/review`：运营/审核角色审核提现申请，通过时可通过 `PLATFORM_PAYOUT_WEBHOOK_URL` 通知外部打款系统并记录通知状态、失败原因和渠道流水号，驳回时退回冻结积分。
- `POST /api/admin/billing/withdrawals/{withdrawal_id}/retry-payout`：运营/审核角色重试已通过提现的外部打款通知，适用于打款通知失败或未配置时的恢复处理。
- `POST /api/admin/billing/payout-webhook/probe`：运营/审核角色发送一条无资金变动的打款 Webhook 探针，用于验证打款系统地址、签名、渠道 payload 和回执解析。
- `GET /api/works`：作品广场列表，支持分类、排序、关键词。
- `GET /api/templates`：可复刻模板/工作流市场列表；内部编排 workflow 保留在 `/api/workflows` 和后台注册表探针中，不作为项目模板复刻。
- `POST /api/interactions`：点赞、收藏、关注。

## 6. ComfyUI 集成边界与工作流

- ComfyUI 默认地址为 `http://127.0.0.1:8188`，通过环境变量覆盖；生产环境支持内网地址、API Key、反向代理和访问白名单。
- 平台 API 支持基础限流，通过 `PLATFORM_RATE_LIMIT_PER_MINUTE` 开启分钟级请求限制，超限返回中文错误。
- 平台 API 支持可选访问令牌，通过 `PLATFORM_API_TOKEN` 保护写接口和 `/api/admin/*` 后台接口；静态前端从 `localStorage.platform_api_token` 读取令牌并透传。
- 平台 API 支持账号注册、密码登录、通用 OAuth/OIDC 第三方登录和轻量 HMAC 会话令牌，通过 `PLATFORM_SESSION_SECRET` 开启；密码使用 PBKDF2 摘要存储，可通过 `PLATFORM_SESSION_TTL_SECONDS` 配置会话有效期，常见创作、任务、互动、发布和后台审核接口可从 `X-User-Session` 补齐用户身份。会话身份由中间件统一解析，若请求体或 query 中显式用户与会话用户不一致会直接拒绝，避免客户端伪造 `user_id` 越权。
- 平台 API 提供 `/api/metrics` Prometheus 文本指标；开启 `PLATFORM_API_TOKEN` 后该监控端点也需要访问令牌。worker 可通过 `--notify-alerts` 将 `/api/health` 同类告警推送到 `PLATFORM_ALERT_WEBHOOK_URL`，可用 `PLATFORM_ALERT_CHANNEL` 选择通用、飞书/Lark、钉钉或 Slack Webhook payload，可用 `PLATFORM_ALERT_WEBHOOK_SECRET` 增加 HMAC 签名，并通过 `PLATFORM_ALERT_COOLDOWN_SECONDS` 与 `PLATFORM_ALERT_STATE_PATH` 对相同告警做指纹冷却降噪。运营后台可发送唯一告警 Webhook 探针，验证机器人地址、渠道 payload 和签名配置。
- FastAPI 可直接托管静态前端目录，并通过 `PLATFORM_CORS_ORIGINS` 配置允许访问 API 的前端域名，便于先用单服务部署 MVP。
- 归档存储支持本地模式和 `PLATFORM_STORAGE_DRIVER=s3` S3/OSS/COS/MinIO 兼容上传模式；对象存储模式保留本地 `local_path` 审计副本，并通过 `PLATFORM_S3_PUBLIC_BASE_URL` 生成公开 URL。`PLATFORM_S3_VENDOR` 支持 `aws-s3`、`aliyun-oss`、`tencent-cos`、`minio` 和 `custom`，配置层会校验 endpoint、bucket、prefix、HTTPS 策略、路径风格和上传超时。运营后台提供存储读写探针，S3 模式会在上传探针后尝试 DELETE 清理远端对象。
- 平台只暴露业务化参数，例如脚本、角色、镜头描述、景别、尺寸、时长、fps、seed、音色、模板 ID；不让前端直接提交任意 ComfyUI 节点图。
- 图像生成：默认接 `selfhost/image_flux.json` 或 `selfhost/image_qwen.json`，输入镜头提示词、角色参考图、尺寸、seed。
- 视频生成：默认接 `selfhost/video_wan2.1_fusionx.json`，输入首帧/参考图、动作描述、时长、fps。
- TTS：默认接 `selfhost/tts_edge.json`，输入旁白文本、音色、语速。
- 分析能力：可接 `analyse_image.json`、`analyse_video.json` 做素材理解，也可接脚本分析类 workflow 生成角色和分镜草稿。
- Workflow Registry 必须为每个 workflow 声明 `workflow_key`、版本、输入 schema、默认值、输出节点映射、生成类型、中文展示名和失败提示。
- 运营后台提供工作流注册表探针，读取所有注册 workflow 并构造示例业务参数，校验 schema、adapter `workflow_key`、输出节点映射、必需生成类型覆盖和 ComfyUI payload 占位符替换结果。
- 任务状态映射为 `pending/running/completed/failed/cancelled`；失败必须记录中文错误、原始 ComfyUI 错误摘要、prompt_id、workflow_key 和可重试建议。
- 默认 inline 模式下任务提交会同步调用 ComfyUI；设置 `PLATFORM_TASK_QUEUE_DRIVER=arq`、`PLATFORM_REDIS_URL` 和 `PLATFORM_TASK_QUEUE_NAME` 后，平台会把任务提交投递到 Redis/arq，并由 `app.backend.worker.WorkerSettings` 中的 worker 函数执行实际 ComfyUI 提交。仓库提供 `deploy/docker-compose.yml`、`deploy/systemd/*` 和 `deploy/env.example` 用于 API、arq worker、巡检 worker、Redis、PostgreSQL 的部署联调。运营后台仍提供运行中任务批量同步接口，并提供 `python -m app.backend.worker` 轻量 CLI 供 cron/systemd 巡检。
- 输出归档流程：平台优先从共享的 ComfyUI 输出目录读取文件；若本地缺文件且输出路径合法，会通过 ComfyUI `/view` 下载远端输出到平台临时区，再计算 hash、识别 mime/尺寸/时长，最后登记为 `Asset` 并绑定项目、分镜、任务或作品。
- ComfyUI 改造不在首版强行 fork 主项目；仓库已提供 `comfyui_plugin/video_gen_platform_nodes` 基础插件包，包含平台业务输入、镜头输入、配音输入、合成清单和平台归档回调节点，并提供 `python -m comfyui_plugin.installer --check` 与 `--comfyui-root` 安装校验工具。后续继续扩展更贴近具体模型和生产 workflow 的素材归档、字幕/TTS 和视频合成节点。

## 7. 前端页面

- `/`：作品广场，顶部分类、搜索、排序，主体作品瀑布流。
- `/create`：高级创建兼容页，支持脚本成片、图片成片、模板复刻、空白项目和 quick 模式参数调整；首页主入口默认直接创建项目并进入全画幅画布。
- `/workspace/:projectId`：全画幅节点画布：左侧搜索式节点添加面板、节点分类筛选、收藏节点、最近使用节点、快速添加上游/下游节点并自动连线、节点面板拖拽添加、节点大纲搜索、问题筛选和批量选中、工作流预设、我的工作流预设、项目分镜面板、分镜状态筛选、分镜关键词搜索、分镜排序、素材类型筛选、素材关键词搜索和任务状态筛选、任务关键词搜索入口，中间无限画布，右侧节点/连线/选区参数面板，支持命令面板搜索、方向键选择和 Enter 执行保存、可持久化、可筛选并可导出的画布事件日志追踪、画布批注节点、版本快照导入导出、工作流追加导入、替换导入和导出、运行、整理、视图和面板开关，支持按单个、选中或筛选后的全部分镜添加生成链路、空白处右键快捷添加节点、工作流预设和粘贴链路、空白处双击创建画布批注节点、拖入本地或 URL 素材、节点拖拽、框选/多选、全选/反选/清空选区、上游/下游链路选择、同类型/同状态/同标记/禁用/孤立/起点/终点/错误/提醒/运行中任务/失败任务节点选择、全选/反选连线、连线同起点/同终点/同标签/同颜色/同样式选择与批量编辑、选区左/右/水平居中/顶部/底部/垂直居中对齐与等距分布、选区打组/选中同组节点/分组名称编辑/取消分组、选区内部连线批量启用/禁用、颜色标记、样式和删除、节点语义输入/输出连接桩连线、连线规则提示、节点右键快捷菜单、连线右键快捷菜单、选区批量操作、选区批量命名、选区批量应用生成参数预设、选中分镜绑定到选区节点、连线标签编辑、复制连线 ID/参数 JSON/定位链接、连线颜色标记、连线样式、连线端口映射、连线插入节点、连线反转方向、连线启用/禁用、全选/反选连线、连线同起点/同终点/同标签/同颜色/同样式选择与批量编辑、节点备注、节点颜色标记、节点折叠/展开、节点参数编辑可撤销、节点类型切换、节点锁定/解锁、节点启用/禁用、自动整理和选区整理、画布自检、运行前缺口提示、撤销/重做、素材预览、素材类型筛选、素材关键词搜索、筛选素材批量添加到画布、任务状态筛选、任务关键词搜索、上游输入预览和参数填充、工作流 JSON 文件追加导入、替换导入和导出、工作流剪贴板导入、选区 JSON 文件导出、自定义预设保存、导入、导出和复用、视口保存/恢复、网格吸附、方向键微调节点位置、节点/选区置顶置底、放大/缩小、适配全部节点、适配选区和重置视口、生成参数预设、保存、单节点运行、当前上游链路运行、选区运行、全图终点链路运行、节点复制、节点/选区断开连线、选区复制/剪切、粘贴到当前视图中心或画布右键位置、选区保存为预设、链路复制粘贴到当前视图中心或画布右键位置、删除、分镜绑定、生成参数编辑、连线补全、节点运行诊断、任务定位、任务状态筛选、任务关键词搜索、任务状态同步、重试和取消。
- `/templates`：模板市场，展示示例封面、成片、适用场景、使用次数。
- `/billing`：积分账户和充值页，查看余额、流水，创建支付订单并在支付渠道返回 `checkout_url` 时打开收银台，开通会员订阅，提交创作者提现申请并由运营审核。
- `/account`：账号令牌页，注册登录、发起第三方登录、刷新会话、校验当前会话并保存平台访问令牌。
- `/account/oauth/callback`：第三方登录回调页，保存后端签发的平台会话并返回账号页。
- `/works/:id`：作品详情，视频播放、描述、作者、模板来源、点赞收藏。
- `/users/:id`：作者主页，作品、模板、作者等级、关注按钮。
- `/admin/review`：发布审核和运营审核队列，仅运营/审核角色可操作，支持作品通过、驳回、下架，可处理待审核提现申请，并提供部署自检、工作流注册表探针、存储读写探针、支付/告警/打款 Webhook 探针和运行中任务同步。

## 8. 交付阶段

- 第 1 阶段：ComfyUI 外接闭环和平台骨架。初始化 Next.js + FastAPI + PostgreSQL + Redis；完成中文 UI 框架、ComfyUI 健康检查、workflow registry、任务提交/查询、输出归档、本地文件存储和基础项目模型。
- 第 2 阶段：核心生成能力。跑通分镜图、镜头视频、TTS、脚本分析四类 workflow；完成任务队列、状态同步、失败重试、中文错误和生成日志。
- 第 3 阶段：创作工作台。完成脚本拆解、角色设定、分镜表格、单镜头重试、批量生成、素材预览、时间线编排、成片合成和导出。已追加完成 LibTV 风格全画幅节点画布，支持平台业务节点编排、节点大纲搜索、问题筛选和批量选中、项目分镜面板、分镜状态筛选、分镜关键词搜索、分镜排序、分镜多选、未铺设分镜一键选择、分镜铺设状态识别与筛选、已铺设分镜链路定位、按单个、选中或筛选后的全部分镜添加生成链路、工作流预设、我的工作流预设、搜索添加、方向键选择节点面板结果、Enter 快速添加当前匹配节点、节点分类筛选、收藏节点、最近使用节点、快速添加上游/下游节点并自动连线、节点面板拖拽添加、节点语义输入/输出连接桩、节点右键快捷操作、连线右键快捷操作、选区批量操作、选区批量命名、选区批量应用生成参数预设、选中分镜绑定到选区节点、连线标签编辑、复制连线 ID/参数 JSON/定位链接、连线颜色标记、连线样式、连线端口映射、连线插入节点、连线反转方向、连线启用/禁用、选区运行、选区一键串联、选区生成视频链、选区生成配音链、选区按媒体类型汇聚到合成节点、选区接入已有合成节点、选区左/右/水平居中/顶部/底部/垂直居中对齐与等距分布、选区打组/选中同组节点/分组名称编辑/取消分组、选区内部连线批量启用/禁用、颜色标记、样式和删除、空白处右键快捷添加节点、工作流预设和粘贴链路、空白处双击添加画布批注、素材拖入、画布内素材预览、素材类型筛选、素材关键词搜索、筛选素材批量添加到画布、任务状态筛选、任务关键词搜索、上游输入预览和参数填充、分镜绑定、参数编辑、节点颜色标记、节点折叠/展开、节点参数编辑可撤销、生成参数预设、连线补全、节点备注、节点类型切换、节点锁定/解锁、节点启用/禁用、节点复制、节点/选区断开连线、选区复制/剪切、粘贴到当前视图中心或画布右键位置、选区保存为预设、链路复制粘贴到当前视图中心或画布右键位置、工作流 JSON 文件追加导入、替换导入和导出、工作流剪贴板导入、选区 JSON 文件导出、视口保存/恢复、视图缩放、视图快速导航、画布视图链接和书签、可导入导出的画布版本快照、画布导航器开关、画布命令面板方向键选择和 Enter 执行、可持久化、可筛选并可导出的画布事件日志、画布批注节点、拖拽框选模式开关、网格吸附、方向键微调节点位置、节点/选区置顶置底、全选/反选/清空选区、上游/下游链路选择、同类型/同状态/同标记/禁用/孤立/起点/终点/错误/提醒/运行中任务/失败任务节点选择、撤销/重做、单节点运行、链路运行、全图运行、自动整理、选区整理、画布自检、运行诊断、任务操作和保存。
- 第 4 阶段：LibTV/Liblib 类社区和模板。完成作品广场、作品详情、作者主页、点赞收藏、模板市场、模板复刻、分类筛选和发布审核。
- 第 5 阶段：ComfyUI 插件化与稳定性。将稳定流程封装为 ComfyUI 自定义节点、插件或工作流包；加入任务取消、后台管理、基础限流、存储清理、权限控制和监控告警。

## 9. 测试计划

- ComfyUI 健康检查：未启动时显示“ComfyUI 未连接”，启动后能读取系统状态。
- Workflow Registry：每个注册 workflow 都能校验输入 schema、默认参数、adapter 输出节点映射和 ComfyUI 提交 payload；前端不能直接依赖节点 ID。
- 生成任务：提交图像/视频/TTS/脚本分析任务后，状态从 `pending` 到 `running` 到 `completed`，失败时返回中文错误、prompt_id 和可重试建议。
- 输出归档：ComfyUI 输出文件能被复制或转存到平台存储，生成 `Asset` 记录并绑定项目、分镜或作品。
- 工作台流程：脚本输入后能生成分镜，单个分镜可重试，批量生成不会阻塞页面；全画幅节点画布可通过项目分镜面板、节点大纲搜索/问题筛选/批量选中、工作流预设、我的工作流预设、搜索面板、方向键选择节点面板结果、Enter 快速添加当前匹配节点、节点分类筛选、收藏节点、最近使用节点、快速添加上游/下游节点并自动连线、节点面板拖拽、空白处右键快捷添加节点、工作流预设和粘贴链路、空白处双击添加画布批注节点或拖入素材添加节点，可按单个、选中或筛选后的全部分镜铺设文本、分镜图、视频、配音和合成链路，可从节点语义输入/输出连接桩连线并阻止自连、重复端口连接，编辑连线标签、复制连线 ID/参数 JSON/定位链接、连线颜色标记、连线样式、调整连线端口映射、反转连线方向、在连线上插入节点、启用/禁用连线、右键定位或删除连线、按同起点/同终点/同标签/同颜色/同样式选中连线并批量编辑、框选/多选节点、全选/反选/清空选区、上游/下游链路选择、同类型/同状态/同标记/禁用/孤立/起点/终点/错误/提醒/运行中任务/失败任务节点选择、全选/反选连线、连线同起点/同终点/同标签/同颜色/同样式选择与批量编辑、节点/选区颜色标记、选区批量命名、选区批量应用生成参数预设、选中分镜绑定到选区节点、节点/选区折叠/展开、复制/剪切选区到系统剪贴板并粘贴到当前视图中心或画布右键位置、保存选区为预设、运行选区、串联选区、选区生成视频链、选区生成配音链、按媒体类型汇聚选区到合成节点、选区接入已有合成节点、整理选区、左/右/水平居中/顶部/底部/垂直居中对齐与等距分布、打组、选中同组节点、编辑分组名称、取消分组、选区内部连线批量启用/禁用、颜色标记、样式和删除、批量锁定/解锁节点、批量启用/禁用节点、自动整理、追加导入、替换导入和导出工作流 JSON 文件、从剪贴板读取工作流 JSON、导出选区 JSON 文件、保存、导入并导出自定义预设、保存画布和视口、保存、恢复、导入并导出画布版本快照、恢复上次画布视口、复制画布视图链接、画布视图书签、命令面板方向键选择和 Enter 执行、可持久化、可筛选并可导出的画布事件日志、画布批注节点、拖拽框选模式开关、网格吸附、方向键微调节点位置、节点/选区置顶置底、放大/缩小、适配全部节点、适配选区和重置视口、为图像/视频/配音/合成生成节点应用参数预设、把选中分镜批量绑定到选区节点、节点参数编辑可撤销、撤销/重做、右键操作节点、编辑节点备注、切换节点类型、锁定/解锁节点、启用/禁用节点、复制节点、复制节点 ID/参数 JSON/定位链接、断开节点/选区连线、复制上游链路到系统剪贴板并粘贴到当前视图中心或画布右键位置、删除、画布自检、运行前缺口提示、素材预览、素材类型筛选、素材关键词搜索、筛选素材批量添加到画布、任务状态筛选、任务关键词搜索、上游输入预览和参数填充、单节点运行、上游链路运行和全画布终点链路运行平台业务节点，阻断性自检问题会在运行前提示并拦截，禁用节点和画布批注节点会在链路和全图运行中被跳过，禁用连线会从上游输入、整理和运行中排除，生成节点可从分镜下拉或上游连线补全业务参数，节点和任务队列能筛选、搜索并追踪任务状态、错误、重试建议、事件日志和 ComfyUI prompt_id，并能提交、同步、重试、取消任务。
- 合成导出：多镜头视频、字幕、配音、BGM 可合成最终 MP4，并生成可访问 URL。
- 社区流程：草稿作品不可见，审核通过后出现在作品广场；点赞、收藏、浏览计数正确。
- 模板复刻：从模板创建项目后，workflow、默认参数、示例提示词能正确带入。
- 权限测试：非作者不能编辑项目；未登录用户只能浏览公开作品。

## 10. 默认假设

- 首版采用混合方案：外接 ComfyUI 先跑通生成闭环，后续再把稳定能力沉淀为 ComfyUI 自定义节点、插件或工作流包。
- 本机 ComfyUI 作为默认生成后端，地址为 `http://127.0.0.1:8188`，生产环境通过环境变量切换到内网 ComfyUI。
- 平台编排层不直接训练或推理模型，只做业务数据、任务编排、状态同步、资产归档、模板和社区能力。
- 首版已提供内部积分账本、生成消耗、运营调账、支付订单、支付收银台 URL 模板、签名支付回调、支付成功幂等入账、支付回调探针、会员订阅、提现申请/审核、提现打款 Webhook 通知审计、打款 Webhook 探针和作品收益分账；真实微信/支付宝/Stripe 账号、第三方收单和提现打款账号仍待后续渠道联调。
- 用户可见内容全部使用简体中文，代码命名保持英文。
